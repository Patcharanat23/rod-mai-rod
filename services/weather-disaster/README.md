# weather-disaster (โมดูล 6)

**หน้าที่**: รวมข้อมูลพยากรณ์อากาศและภัยพิบัติจากหลายแหล่ง แปลงให้อยู่ในรูปแบบเดียวกัน แล้ว cache
**ผู้รับผิดชอบ**: TBD
**branch**: `feature/weather-disaster/<ชื่อ>`
**endpoint**: `POST /api/v1/forecast/points`, `GET /api/v1/area`, `GET /api/v1/hazards` (`docs/CONTRACT.md` หัวข้อ 6)
**เรียกใคร**: Open-Meteo, GDACS, USGS, Thaiwater (ถ้าได้ key), TMD (ถ้าได้ key)
**ใครเรียกเรา**: risk-decision, api-backend

## รันเดี่ยว

```bash
pip install -r requirements.txt
uvicorn app:app --reload --port 8000
```

## จุดที่คนส่วนใหญ่พลาด (อ่านก่อนเขียน)

1. **ใช้อากาศตอนนี้แทนพยากรณ์ ณ เวลาที่ไปถึง** `forecast/points` รับ `time` มาทุกจุด ต้องเลือกค่ารายชั่วโมงของชั่วโมงนั้น ไม่ใช่ `current`
2. **timezone ของ Open-Meteo** ถ้าไม่ระบุ ค่าเวลาที่ได้จะเป็น GMT แต่ถ้ามีคนใส่ `timezone=Asia/Bangkok` เวลาในคำตอบจะเลื่อนไป 7 ชม. เลือกอย่างใดอย่างหนึ่งแล้วแปลงเป็น UTC ให้ถูกก่อนจับคู่กับเวลาที่ขอ
3. **ขอพยากรณ์เกินระยะที่มี** พยากรณ์รายชั่วโมงมีล่วงหน้าไม่กี่วัน (ค่าเริ่มต้น 7 วัน) ถ้าวันที่ขอเกิน ให้ตอบ warning `FORECAST_OUT_OF_RANGE` ห้าม error และห้ามใส่ค่าปลอม
4. **หน่วยไม่ตรงกัน** ลมต้องเป็น กม./ชม. ฝนต้องเป็น มม./ชม. บางแหล่งให้ลมเป็น ม./วินาที (คูณ 3.6) ระบุหน่วยในคำขอ Open-Meteo ให้ชัด แปลงที่นี่ที่เดียว คนอื่นเชื่อหน่วยของเรา
5. **ไม่ cache** จะโดนจำกัดโควตาเร็วมาก ใช้ key = พิกัดปัดทศนิยม 2 ตำแหน่ง (ประมาณ 1 กม.) + ชั่วโมง เก็บไว้ประมาณ 30 นาที และขอหลายจุดในคำขอเดียวเท่าที่ API รองรับ
6. **แหล่งหนึ่งพังแล้วพังหมด** แต่ละแหล่งห่อ try/except แยกกัน ตั้ง timeout 8 วินาที แหล่งไหนล่มก็ส่งจากแหล่งที่เหลือพร้อม `HAZARD_FEED_UNAVAILABLE`
7. **หมุดภัยนอกประเทศเต็มแผนที่** GDACS และ USGS เป็นข้อมูลทั้งโลก กรองด้วยกรอบประเทศไทย (ขยายขอบนิดหน่อยสำหรับแผ่นดินไหวใกล้ชายแดน) และกรองแผ่นดินไหวที่เล็กเกินไปทิ้ง
8. **ไม่มีแหล่งข้อมูลดินถล่มโดยตรง** หน้า Safety Map ต้องมีไอคอนดินถล่ม ให้ประเมินเองจากฝนสะสม 24 ชม. สูงในพื้นที่ภูเขา แล้วตั้ง `hazard_type: LANDSLIDE_RISK` + `source: DERIVED` และเขียนชื่อให้รู้ว่าเป็นการประเมิน เช่น "เสี่ยงดินถล่ม (ประเมินจากปริมาณฝน)" ห้ามทำให้ดูเหมือนเป็นประกาศทางการ
9. **รอ key Thaiwater / TMD แล้วงานค้าง** สองแหล่งนี้เป็นของเสริม ไม่ใส่ key = ข้ามไปเลย ระบบต้องทำงานได้ครบด้วย Open-Meteo + GDACS + USGS
10. **พิกัดเรียงผิด** GDACS/USGS ส่ง GeoJSON `[lng, lat]` แปลงเป็น `{lat, lng}` ก่อนส่งออกเสมอ

## ถือว่าเสร็จเมื่อ

- ขอพยากรณ์ 20 จุดตามเส้นทางกรุงเทพ-เชียงใหม่ได้ในไม่กี่วินาที และขอซ้ำรอบสองเร็วขึ้นชัดเจน (cache ทำงาน)
- ปิดเน็ตหรือทำให้แหล่งหนึ่ง timeout แล้วยังได้คำตอบพร้อม warning
- `DEMO_MODE=true` ตอบทริปตัวอย่างได้โดยไม่ต่อเน็ต
